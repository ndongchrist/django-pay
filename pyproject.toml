[project]
name = "djangopayment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.3",
    "pre-commit>=4.5.1",
    "ruff>=0.15.7",
]


[tool.ruff]
line-length = 120

[tool.black]
line-length = 120
